<%= form_with(model: builder) do |form| %>
  <% if builder.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(builder.errors.count, "error") %> prohibited this builder from being saved:</h2>

      <ul>
        <% builder.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :clientName, style: "display: block" %>
    <%= form.text_field :clientName %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
