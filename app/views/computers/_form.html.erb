<%= form_with(model: computer) do |form| %>
  <% if computer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(computer.errors.count, "error") %> prohibited this computer from being saved:</h2>

      <ul>
        <% computer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :clientName, style: "display: block" %>
    <%= form.text_field :clientName %>
  </div>

  
   <%= form.collection_select :component1, Component.all, :componentName, :componentName, prompt: 'Select a category' %>
   <%= form.collection_select :component2, Component.all, :componentName, :componentName, prompt: 'Select a category' %>
   <%= form.collection_select :component3, Component.all, :componentName, :componentName, prompt: 'Select a category' %>
   <%= form.collection_select :component4, Component.all, :componentName, :componentName, prompt: 'Select a category' %>
  <%= form.number_field :qntOfSlot%>


  <div>
    <%= form.submit %>
  </div>

<% end %>
