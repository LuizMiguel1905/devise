<%= form_with(model: pc_building) do |form| %>
  <% if pc_building.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pc_building.errors.count, "error") %> prohibited this pc_building from being saved:</h2>

      <ul>
        <% pc_building.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :clientName, style: "display: block" %>
    <%= form.text_field :clientName %>
  </div>
<div>
    <%= form.collection_select :component_id_1, Component.all, :id, :componentName, prompt: true %>
  </div>

  <div>
    <%= form.collection_select :component_id_2, Component.all, :id, :componentName, prompt: true %>
  </div>

  <div>
    <%= form.collection_select :component_id_3, Component.all, :id, :componentName, prompt: true %>
  </div>

  <div>
    <%= form.collection_select :component_id_4, Component.all, :id, :componentName, prompt: true %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
